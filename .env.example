# Optional environment overrides for examples.
# Copy to `.env` and adjust as needed.

# Used by GetEnvHarness / general config demos.
NETWORK=local

# RPC overrides for fork examples. Defaults are public meowrpc endpoints.
ETH_RPC_URL=https://eth.meowrpc.com
BASE_RPC_URL=https://base.meowrpc.com
